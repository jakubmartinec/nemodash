import { useTranslations } from "next-intl";

export default function DashboardPage() {
  // Placeholder — komponenty budou přidány v dalším kroku
  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">Dashboard</h1>
    </main>
  );
}
